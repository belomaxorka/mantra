# --- Apache & Nginx (production profile) ---
# Domain for HTTPS via Caddy reverse proxy
# DOMAIN=example.com

# --- Caddy variant ---
# Site address: ":80" for development, "example.com" for production HTTPS
# SITE_ADDRESS=:80
# HTTP port: 8080 for development, 80 for production
# HTTP_PORT=8080
